# 《校园食光》管理员权限与层级规范（V1 · 2026-02-01）

> 目标：让“创建管理员 / 调岗 / 授权”可复用、可扩展、可审计，并且**永不越权**。

---

## 0. 术语与核心原则

### 0.1 术语

- **角色（Role / Level）**：平台超级管理员 / 学校管理员 / 商户管理员 / 档口管理员。
- **作用域（Scope）**：管理员可管理的组织范围。
- **节点（Node）**：学校、商户树节点、档口等组织节点。
- **权限（Permission）**：细粒度操作能力，用 `模块.动作` 命名。

### 0.2 核心原则（必须满足）

1. **无注册入口**：管理员账号禁止注册，只能由上级创建或后续绑定。
2. **可先建框架后绑定人**：学校/商户/档口等节点允许“无管理员绑定”创建。
3. **可调岗**：管理员账号可从 A 节点解绑并绑定到 B 节点（保留历史审计）。
4. **不可越权**：任何授权、调岗、创建都必须在操作者权限和作用域允许范围内。

---

## 0.3 讨论结论（待定稿）- 多职位/多绑定与导航

> 本节是当前讨论达成的关键“产品规则”，用于指导前端交互与后端权限校验；后续可整理进定稿章节。

### 0.3.1 一人多位置（多绑定）

- 允许一个管理员账号绑定多个位置（例如：同一学校内可同时绑定“一饭”与“二饭>三楼”等不同层级节点）。
- **跨校多绑定禁止**：同一账号不得绑定不同 `school_id` 的位置。
  - 平台超级管理员（L0）属于平台作用域，不通过“多校绑定”实现跨校操作；跨校分配由 L0 在系统侧管理，但账号本身不绑定多校。

### 0.3.2 顶部导航栏：职位切换（Position Switcher）

- 管理端顶部增加“我的职位/我的岗位”入口。
- 若账号只有一个绑定位置：只展示该职位标签。
- 若账号有多个绑定位置：下拉展示多个职位，切换后进入对应控制台首页（例如 `super_admin_index.html` / `school_admin_index.html` / `merchant_admin_index.html` / `stall_dashboard.html`）。
- 切换职位本质是切换“当前激活作用域”（active scope），页面数据与可见按钮应随之刷新。

### 0.3.3 同级管理（peer manage）默认关闭

- “能否管理同级管理员”是一个可选细分权限，**默认关闭**。
- 交互建议：默认只展示常用选项；点击“细分/高级”按钮展开后才能勾选“管理同级”。

### 0.3.4 列表可见性与调岗可见性（你强调的“怎么呈现在列表里”）

- 管理员在列表中的出现逻辑与“绑定位置”强相关：
  - 默认只在其绑定位置所属作用域列表中出现；
  - 其上级链路（角色更高且作用域覆盖该位置）始终可见该账号。
- 调岗后，账号随新的绑定位置进入对应列表；同时仍满足“上级覆盖者可见”。
- 组织树可见性建议：
  - 自己负责的节点：可见完整子树。
  - 同级节点：可见“节点概览”（只看到同级有哪些），默认不允许下钻查看其子树。


---

## 1. 角色层级与作用域模型

### 1.1 角色层级（从高到低）

- **L0：平台超级管理员（super_admin）**
- **L1：学校管理员（school_admin）**
- **L2：商户管理员（merchant_admin）**
- **L3：档口管理员（stall_admin）**

### 1.2 作用域（Scope）建议字段

- 平台：`scope_type=platform`（全局）
- 学校：`scope_type=school` + `school_id`
- 商户树：`scope_type=merchant_node` + `merchant_node_id`（含子树）
- 档口：`scope_type=stall` + `stall_id`

> 说明：L2 的商户结构是 **1-N 级树**，本规范用 `merchant_node` 统一表示“商户/楼层/区域”等任意层级节点。

### 1.3 商户 1-N 级树规则

- 商户树节点可多级：例：一饭（一级）→ 三楼（二级）→ 某档口（三级）。
- **档口必须挂在某个商户树节点的下一级**（不允许孤立档口）。
- 商户管理员（L2）管理范围 = 自己绑定的 `merchant_node` **及其整棵子树**。
- 学校管理员（L1）管理范围 = 本校下所有商户树节点与档口。

---

## 2. ✅ 权限清单（按模块拆分，含操作细节）

统一命名：权限以 `模块.动作` 表示，便于前后端对齐。

### A. 组织与层级（org.*）

- `org.school.create` 创建学校
- `org.school.update` 编辑学校信息
- `org.school.disable` 停用学校
- `org.merchant.create` 创建商户节点（任意层级）
- `org.merchant.update` 编辑商户节点
- `org.merchant.move` 调整商户层级/归属
- `org.merchant.disable` 停用商户节点
- `org.stall.create` 创建档口
- `org.stall.update` 编辑档口
- `org.stall.move` 调整档口归属
- `org.stall.disable` 停用档口

**操作细节**

- 允许“先建框架、后绑定人”。
- 新建学校/商户/档口可不绑定管理员账号，后续在“人员配置”里补绑定。
- 允许调岗：管理员可从 A 节点解绑，再绑定到 B 节点（保留历史审计）。

### B. 人员与权限（admin.*）

- `admin.create` 新建管理员
- `admin.update` 编辑管理员资料
- `admin.disable` 禁用管理员
- `admin.assign` 绑定管理员到节点（含调岗）
- `admin.unassign` 解绑管理员
- `admin.grant` 授权/调整权限
- `admin.role` 调整管理员层级
- `admin.manage_peer` 管理同级管理员（高级权限，默认关闭）
- `admin.identity.approve` 审核身份标签后缀
- `admin.audit` 查看管理员操作日志

**操作细节**

- 权限继承规则：新管理员权限 = 当前操作者权限 ∩ 目标角色模板权限。
- 禁止越权：不能授予自己没有的权限。
- 不可自授权：操作者不能修改自己的权限，不能解绑自己。
- 同级可建：允许同级创建，但仅限自己作用域内（同校 / 同商户树），需要 `admin.manage_peer` 权限。
- 管理员账号不可注册，只能由上级创建或绑定。
- 禁用管理员：账号状态标记为 `disabled`，权限立即失效，但历史数据与审计日志保留不删除。

### C. 运营内容（content.*）

- `content.announce.create` 创建公告
- `content.announce.update` 编辑公告
- `content.announce.publish` 发布/下架
- `content.ai.generate` 使用 AI 生成内容
- `content.topic.manage` 管理话题/标签

### D. 审核与风控（review.*）

- `review.audit` 审核评论/动态/评价
- `review.reject` 拒绝并给原因
- `review.delete` 删除内容
- `review.sensitive.manage` 管理敏感词库
- `review.blacklist` 拉黑用户

### E. 业务数据与报表（data.*）

- `data.dashboard.view` 查看看板
- `data.report.view` 查看报表
- `data.export` 导出数据

### F. 菜品与库存（dish.* / stock.* / price.*）

- `dish.create` 新建菜品
- `dish.update` 编辑菜品
- `dish.delete` 删除菜品
- `stock.mode` 切换库存模式
- `stock.update` 调整库存
- `price.update` 修改价格

---

## 3. ✅ 各角色默认权限模板（满配）

> 真实授予时仍需“与操作者权限取交集”。

### 3.1 平台超级管理员（L0）

- 全部权限。

### 3.2 学校管理员（L1）

- 组织：学校内所有商户树节点/档口管理（`org.merchant.*`, `org.stall.*`，以及必要的 `org.school.update`）
- 人员：可创建学校管理员、商户管理员、档口管理员（`admin.create`, `admin.assign`, `admin.grant`, `admin.disable`, `admin.audit`）
- 内容/审核：本校范围（`content.*`, `review.*`）
- 数据：本校范围（`data.*`）
- 菜品：本校范围（`dish.*`, `stock.*`, `price.*`）

### 3.3 商户管理员（L2）

- 组织：本商户树范围节点管理（`org.merchant.*`, `org.stall.*` 在子树内）
- 人员：可创建商户管理员、档口管理员（`admin.create`, `admin.assign`, `admin.grant`, `admin.disable`, `admin.audit`，但仅限子树作用域）
- 内容/审核：本商户树范围（`content.*`, `review.*` 限定范围）
- 数据：本商户树范围（`data.*`）
- 菜品/库存：本商户树范围（`dish.*`, `stock.*`, `price.*`）

### 3.4 档口管理员（L3）

- 菜品/库存：本档口（`dish.*`, `stock.*`, `price.*`）
- 评价/口碑处理：本档口（`review.audit`, `review.reject`, `review.delete` 若需要可拆更细）
- 数据查看：本档口（`data.dashboard.view`, `data.report.view`）
- 默认不含创建管理员权限（除非显式授予 `admin.create`）。

---

## 3.5 身份标签系统（用于社区展示与后台管理辅助）

### 3.5.1 设计目标

管理员在社区评论/发帖时需要展示其身份，同时后台管理员列表也需要通过身份标签辅助查找和管理。

### 3.5.2 身份标签组成

**完整展示格式**：`用户名（身份标签 · 自定义后缀）`

- **身份标签**（必填）：固定 3 个选项
  - 教职工：对应教师、校领导、行政人员、后勤老师等有编制身份
  - 工作人员：对应食堂阿姨、保洁、食堂后勤、商户服务人员等一线服务/运营身份
  - 运营专员：对应平台管理员、校外合作、技术运维等专业管理身份

- **自定义后缀**（可选）：可由管理员创建时填写或后续自己申请
  - 示例：主播、审核员、技术负责人、一饭负责人、麻辣烫档口长等
  - 用途：精确展示职责，帮助用户识别管理员角色
  - **需审核**：自定义后缀需上级管理员（或具备 `admin.identity.approve` 权限者）审核通过后才展示

### 3.5.3 后缀审核流程

1. **快路径**：创建管理员时由创建者直接填写 → 立即生效
2. **慢路径**：管理员在"个人中心"提交后缀申请 → 上级审核 → 通过后生效

- 审核前状态：后台列表与社区评论区均不显示后缀，只显示身份标签
- 审核通过：后缀立即在后台和社区全部生效
- 审核拒绝：后缀申请失效，管理员可重新提交

### 3.5.4 身份标签在后台管理中的作用

- **列表展示**：管理员列表每一行显示「用户名 | 身份标签 · 后缀 | 职位」
- **排序规则**：
  1. 未分配职位的排最前（绿色高亮）
  2. 已分配的按身份标签分组：相同标签排在一起
  3. 同标签内：先按标签字数（含后缀）升序，再按字典序
  4. 同标签同字数：按名字字数升序，再按字典序
- **辅助查找**：通过身份标签快速识别"谁是教职工/工作人员/运营专员"，避免只看名字盲找

---

## 4. ✅ 关键流程（给前端/后端对齐）

### 4.1 新建节点（学校/商户树节点/档口）

- 允许“无管理员绑定”。
- 创建后在“人员配置”页绑定管理员。
- 绑定校验：
  - **作用域校验**：必须落在操作者作用域内。
  - **权限校验**：需要对应 `org.*.create` 或 `admin.assign`。

### 4.2 新建管理员（建议做成“通用创建”，不繁琐）

**极简创建（默认推荐）**

1. 选择角色（L0/L1/L2/L3）
2. 选择作用域节点（platform/school/merchant_node/stall）
3. 一键创建

**权限规则**

- 默认权限：`操作者权限 ∩ 角色模板权限`
- 若提供“高级设置”：
  - 可从默认权限里删减（不可新增超出操作者权限的项）

### 4.3 调岗 / 解绑 / 重新绑定

- 解绑不删除账号，只移除管理范围。
- 允许重新绑定到新节点，权限可重新裁剪。
- 调岗校验建议：
  - 必须具备 `admin.assign` 权限。
  - 目标节点必须在操作者作用域内。
  - 目标节点层级必须为“同级或下级”（可配置为严格只下级，视产品选择）。

---

## 5. 建议的 UI 交互（让组员好落地）

### 5.1 “新增管理员”弹窗（默认简洁）

- **基础信息**：
  - 用户名、真实姓名、手机、邮箱
  - **身份标签**（必填）：教职工 / 工作人员 / 运营专员
  - **自定义后缀**（可选）：创建者可直接填写，立即生效（快路径）
- **角色**：下拉选择（super_admin / school_admin / merchant_admin / stall_admin）
- **作用域**：树选择（只展示操作者可选范围）
- **权限**：默认不展示（或仅展示"继承权限预览"）
- **高级设置**（可折叠）：权限勾选（只能从默认权限里取消，不可新增）

### 5.2 "调岗/绑定"弹窗

- 当前绑定：展示（学校/商户树节点/档口）
- 目标绑定：树选择（只展示操作者可调范围）
- 变更原因：可选（用于审计）
- 支持拖动：可直接拖动管理员行到目标节点，松手后弹出确认弹窗

### 5.3 管理员列表（树状展示 + 搜索 + 排序）

**列表布局（树状缩进）**

```
[搜索框：输入管理员姓名前缀搜索] [我的创建]

[未分配职位]（折叠/展开）
  ├─ 张三 | 教职工 | 未分配 （绿色高亮）
  └─ 李四 | 工作人员 · 主播 | 未分配 （绿色高亮）

XX大学（L1 学校）
  ├─ 王五 | 运营专员 | 学校管理员
  └─ 一饭（L2 商户节点1）
      ├─ 赵六 | 教职工 · 审核员 | 商户管理员
      └─ 楼层A（L2 商户节点2）
          ├─ 孙七 | 工作人员 | 商户管理员
          └─ 麻辣烫档（L3 档口）
              └─ 周八 | 工作人员 · 档口长 | 档口管理员

[展开同级]（按钮）
  └─ 二饭（灰色，点击提示无权限）
```

**排序规则（组内排序）**

1. 未分配职位的排最前（绿色高亮）
2. 已分配的按身份标签分组：教职工 → 工作人员 → 运营专员
3. 同标签内：
   - 先按「标签+后缀」总字数升序
   - 再按字典序
4. 同标签同字数：
   - 按名字字数升序
   - 再按字典序

**颜色与状态标识**

| 状态 | 背景色 | 行内容 |
|------|--------|--------|
| 未分配职位 | 绿色/黄色高亮 | 用户名 \| 身份标签 \| 未分配 |
| 已分配（活跃） | 正常白色 | 用户名 \| 身份标签 · 后缀 \| 职位 |
| 已禁用 | 灰色 | 用户名 \| 身份标签 \| 职位 \| [已禁用] |
| 后缀待审核 | 正常白色 | 用户名 \| 身份标签 \| 职位（后缀不显示）|

**搜索跳转功能**

- 输入框：支持管理员姓名前缀搜索（例如输入"刘"，匹配所有"刘XX"开头的）
- 搜索结果：弹出下拉列表，显示匹配的管理员及其当前职位
- 点击结果：
  - 自动展开对应层级树节点
  - 高亮显示该管理员所在行（框框或闪烁）
  - 滚动列表使其出现在可视区域

**"我的创建"窗口**

点击"我的创建"按钮，弹出侧边栏或对话框：

```
[我创建的管理员]（按创建时间倒序）

1. 张三 - 商户管理员 - 一饭>楼层A - 2026-02-01 14:30
2. 李四 - 档口管理员 - 一饭>楼层A>麻辣烫档 - 2026-02-01 12:00
3. 王五 - 未分配 - 2026-01-31 10:00
```

点击某一行：
- 跳转到主列表对应层级
- 高亮显示该管理员（与搜索跳转一致）

**同级节点展示（默认隐藏）**

- 默认不显示同级商户节点
- 点击"展开同级"按钮后：
  - 显示同级商户节点名称（灰色不可点击）
  - 不展示同级节点的子商户、子档口
  - 同级档口始终不显示（档口只能在其所属商户节点下看到）
- 点击同级节点：弹窗提示"无权限查看该区域"

**拖动调岗**

- 鼠标按住管理员行不放，拖动到目标节点上
- 松手后弹出"调岗确认"对话框，显示：
  - 当前绑定：XX节点
  - 目标绑定：YY节点
  - 变更原因：[可选输入]
- 确认后执行调岗，刷新列表

---

## 6. 需要团队确认的两处产品开关（建议你拍板）

1. **同级调岗是否允许**：
   - 方案A（更灵活）：允许同级 + 下级（你刚刚倾向的方案）
   - 方案B（更严）：只允许下级
2. **档口管理员是否允许创建管理员**：
   - 默认不允许，除非显式授予 `admin.create`

---

## 7. 与现有前端实现的关系（现状提示）

当前前端已有基于角色的粗粒度校验逻辑（例如角色层级判断、页面访问白名单、按 `schoolId/merchantId/stallId` 过滤数据）。
本规范是在此基础上补齐“细粒度权限清单 + 继承/裁剪/调岗规则”，后续可以逐步把页面按钮和接口都按 `模块.动作` 做显隐和校验。
---

## 8. 数据库表设计（后端实现参考）

### 8.1 管理员账号表 `admin_users`

```sql
CREATE TABLE admin_users (
  id INT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(50) UNIQUE NOT NULL COMMENT '登录用户名',
  password_hash VARCHAR(255) NOT NULL,
  real_name VARCHAR(50) COMMENT '真实姓名',
  phone VARCHAR(20),
  email VARCHAR(100),
  
  -- 身份标签（用于社区展示与后台管理）
  identity_label ENUM('教职工', '工作人员', '运营专员') NOT NULL COMMENT '固定身份标签',
  identity_suffix VARCHAR(100) DEFAULT NULL COMMENT '自定义后缀',
  identity_suffix_status ENUM('draft', 'pending_review', 'approved', 'rejected') DEFAULT 'draft' COMMENT '后缀审核状态',
  identity_suffix_approver_id INT DEFAULT NULL COMMENT '后缀审核人ID',
  identity_suffix_approved_at DATETIME DEFAULT NULL COMMENT '后缀审核通过时间',
  
  status ENUM('active', 'disabled') DEFAULT 'active' COMMENT '账号状态',
  created_by INT COMMENT '创建者ID',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  
  INDEX idx_status (status),
  INDEX idx_identity_label (identity_label),
  INDEX idx_created_by (created_by)
) COMMENT='管理员账号表';
```

### 8.2 职位绑定表 `admin_positions`

记录管理员与组织节点的绑定关系，支持一人多职位（同校内）。

```sql
CREATE TABLE admin_positions (
  id INT PRIMARY KEY AUTO_INCREMENT,
  admin_id INT NOT NULL COMMENT '管理员ID',
  
  -- 角色与作用域
  role ENUM('super_admin', 'school_admin', 'merchant_admin', 'stall_admin') NOT NULL,
  school_id INT DEFAULT NULL COMMENT '学校ID（NULL表示平台级）',
  merchant_node_id INT DEFAULT NULL COMMENT '商户树节点ID（NULL表示学校级/平台级）',
  stall_id INT DEFAULT NULL COMMENT '档口ID（NULL表示非档口级）',
  
  -- 绑定与解绑记录
  assigned_by INT NOT NULL COMMENT '绑定操作者ID',
  assigned_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  unassigned_at DATETIME DEFAULT NULL COMMENT 'NULL表示当前生效',
  unassigned_by INT DEFAULT NULL,
  unassign_reason VARCHAR(255) COMMENT '解绑原因（用于审计）',
  
  INDEX idx_admin_active (admin_id, unassigned_at),
  INDEX idx_school (school_id),
  INDEX idx_merchant (merchant_node_id),
  INDEX idx_stall (stall_id),
  CONSTRAINT fk_admin FOREIGN KEY (admin_id) REFERENCES admin_users(id)
) COMMENT='管理员职位绑定表（支持多职位、历史记录）';
```

### 8.3 权限授予表 `admin_permissions`

```sql
CREATE TABLE admin_permissions (
  id INT PRIMARY KEY AUTO_INCREMENT,
  position_id INT NOT NULL COMMENT '职位ID（关联 admin_positions.id）',
  permission VARCHAR(100) NOT NULL COMMENT '权限标识，如 admin.create',
  
  granted_by INT NOT NULL COMMENT '授权人ID',
  granted_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  revoked_at DATETIME DEFAULT NULL,
  
  INDEX idx_position (position_id),
  INDEX idx_permission (permission),
  CONSTRAINT fk_position FOREIGN KEY (position_id) REFERENCES admin_positions(id)
) COMMENT='管理员权限授予表';
```

### 8.4 角色模板权限表 `role_template_permissions`

存储各角色的默认权限模板，便于产品迭代统一升级。

```sql
CREATE TABLE role_template_permissions (
  id INT PRIMARY KEY AUTO_INCREMENT,
  role ENUM('super_admin', 'school_admin', 'merchant_admin', 'stall_admin') NOT NULL,
  permission VARCHAR(100) NOT NULL COMMENT '权限标识',
  description VARCHAR(255) COMMENT '权限说明',
  is_default TINYINT(1) DEFAULT 1 COMMENT '是否默认启用',
  
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  
  UNIQUE KEY uk_role_perm (role, permission)
) COMMENT='角色权限模板（产品配置）';
```

### 8.5 审计日志表 `admin_audit_log`

```sql
CREATE TABLE admin_audit_log (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  operator_id INT NOT NULL COMMENT '操作者ID',
  operator_position_id INT COMMENT '操作时使用的职位ID',
  
  action VARCHAR(100) NOT NULL COMMENT '操作类型：create_admin/assign_position/grant_permission等',
  target_type VARCHAR(50) COMMENT '操作目标类型：admin/position/school/merchant等',
  target_id INT COMMENT '操作目标ID',
  
  before_data JSON COMMENT '操作前数据快照',
  after_data JSON COMMENT '操作后数据快照',
  reason VARCHAR(255) COMMENT '操作原因',
  ip_address VARCHAR(50),
  user_agent VARCHAR(255),
  
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  
  INDEX idx_operator (operator_id, created_at),
  INDEX idx_target (target_type, target_id),
  INDEX idx_action (action, created_at)
) COMMENT='管理员操作审计日志';
```

---

## 9. 技术实现要点（前后端对齐）

### 9.1 Token 与多职位切换

**方案：Token 包含管理员身份，前端传递激活职位ID**

```javascript
// 登录成功后，后端返回
{
  "token": "jwt_token_here",
  "adminId": 123,
  "positions": [
    {
      "id": 456,
      "role": "school_admin",
      "label": "XX大学 · 学校管理员",
      "schoolId": 10,
      "target": "/main/admin/school_admin_index.html"
    },
    {
      "id": 789,
      "role": "merchant_admin",
      "label": "XX大学 · 一饭 · 商户管理员",
      "schoolId": 10,
      "merchantNodeId": 20,
      "target": "/main/admin/merchant_admin_index.html"
    }
  ]
}

// 前端存储
localStorage.setItem('adminToken', token);
localStorage.setItem('adminPositions', JSON.stringify(positions));
localStorage.setItem('adminActivePositionId', positions[0].id); // 默认激活第一个
```

**前端每次请求时传递激活职位**

```javascript
const headers = {
  'Authorization': 'Bearer ' + localStorage.getItem('adminToken'),
  'X-Active-Position-Id': localStorage.getItem('adminActivePositionId')
};
```

**后端中间件处理**

```javascript
// 解析 token 拿到 admin_id
// 从 header 读取 X-Active-Position-Id
// 查询 admin_positions 表确认该 position 属于该 admin 且 unassigned_at IS NULL
// 加载该 position 的 role、scope、permissions
// 将权限信息挂载到 req.adminContext 供后续接口校验
```

### 9.2 权限校验函数（后端中间件示例）

```javascript
function requirePermission(permission) {
  return (req, res, next) => {
    const { adminContext } = req; // 从上一步中间件获取
    if (!adminContext || !adminContext.permissions.includes(permission)) {
      return res.status(403).json({ error: '无权限操作' });
    }
    next();
  };
}

// 使用示例
router.post('/api/admin/create', requirePermission('admin.create'), createAdminHandler);
```

### 9.3 作用域数据过滤（后端查询示例）

```javascript
function filterByScope(query, adminContext) {
  const { role, schoolId, merchantNodeId, stallId } = adminContext;
  
  if (role === 'super_admin') {
    // 平台级，不过滤
    return query;
  } else if (role === 'school_admin') {
    // 学校级，只查本校数据
    return query.where('school_id', schoolId);
  } else if (role === 'merchant_admin') {
    // 商户级，查本商户树子树数据
    const subNodes = await getMerchantSubTree(merchantNodeId);
    return query.whereIn('merchant_node_id', subNodes);
  } else if (role === 'stall_admin') {
    // 档口级，只查本档口
    return query.where('stall_id', stallId);
  }
}
```

### 9.4 身份标签后缀审核流程

**管理员申请后缀**

```sql
UPDATE admin_users 
SET identity_suffix = '主播', 
    identity_suffix_status = 'pending_review'
WHERE id = ?;
```

**上级审核通过**

```javascript
router.post('/api/admin/:id/approve-suffix', requirePermission('admin.identity.approve'), async (req, res) => {
  const { id } = req.params;
  const { approved } = req.body; // true or false
  
  await db('admin_users')
    .where('id', id)
    .update({
      identity_suffix_status: approved ? 'approved' : 'rejected',
      identity_suffix_approver_id: req.adminContext.adminId,
      identity_suffix_approved_at: approved ? new Date() : null
    });
  
  res.json({ success: true });
});
```

---